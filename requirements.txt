# 데이터 수집 (한국투자증권 KIS Open API 단일 출처)
httpx>=0.27.0

# 웹 크롤링 (테마/섹터)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# 데이터 처리
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=14.0.0           # parquet I/O

# 스토리지
# sqlite3는 표준 라이브러리

# 알림
python-telegram-bot>=20.7  # or use raw requests

# PWA 대시보드 (M7)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0   # websockets 의존성도 함께 설치

# 환경설정
python-dotenv>=1.0.0
pyyaml>=6.0

# 스케줄링
apscheduler>=3.10.0

# 재시도 / 진행률
tenacity>=8.2.0
tqdm>=4.66.0

# 로깅
loguru>=0.7.0

# 시간대
pytz>=2023.3

# 테스트 (개발용)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
